import styled from "styled-components"

export const DivDisplayProdutos = styled.div`
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
  padding: 10px;`

export const DivSelects = styled.div`
  display: flex;
  justify-content: space-between;
  width: 700px;
  align-items: center;
  gap: 50px;
  margin: 0 auto;
  `